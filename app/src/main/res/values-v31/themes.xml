<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="Theme.MessageApp" parent="Base.Theme.MessageApp">
        <!-- 스플래시 아이콘/배경/브랜딩 제거 -->
        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:windowSplashScreenBackground">@android:color/transparent</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@null</item>
        <item name="android:windowSplashScreenBrandingImage">@null</item>
        <!-- ... 기존 앱 테마 설정 ... -->
        <item name="postSplashScreenTheme">@style/Theme.MessageApp</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/main_navy</item>

    </style>
</resources>