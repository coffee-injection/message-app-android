<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 클릭후(눌림) -->
    <item android:state_pressed="true" android:color="@color/blue_800"/>
    <!-- 클릭전(기본) -->
    <item android:color="@color/stroke_dark"/>
</selector>
